<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".fairystory.FairyStoryApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">

        <activity
            android:name=".fairystory.ui.LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".fairystory.ui.CardControllerActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".fairystory.ui.ThemeIntroduceActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".fairystory.ui.ThemesActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".fairystory.ui.RuntimeActivity"
            android:launchMode="singleTask" />

        <activity
            android:name="com.bearya.robot.fairystory.ui.StationActivity"
            android:launchMode="singleTask" />

        <activity
            android:name="com.bearya.robot.fairystory.ui.station.ContentActivity"
            android:launchMode="singleTask" />

    </application>

</manifest>